# Copy to .env and adjust. Never commit .env.
# This project needs no API keys: retrieval runs entirely on local models.

RAG_DATASET=scifact
RAG_CHUNKING=whole      # whole | sentence3 | window120
RAG_RETRIEVER=hybrid    # bm25 | dense | hybrid
